<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sardinha.iohan.enem_quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".SugerirQuestaoActivity" />
        <activity android:name=".RegrasActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".AprovarQuestoesActivity" />
        <activity android:name=".RedefinirSenhaActivity"></activity>
    </application>

</manifest>